# AI Development Log

This log documents our use of AI and agentic coding tools during the development of Luma (Loan Data Verification Copilot), as required by the hackathon prompt.

> [!IMPORTANT]
> This is a **live document** — append prompts, decisions, and rejected outputs as they happen during Phases 0–5. Do not backfill at hour 45. §10 requires 5–10 representative prompts and ≥2 rejected-output examples; judges score authenticity.

## 0. Development Timeline (verified via git history)

| Phase | Branch | Key commits (verified) |
|---|---|---|
| 0–1 | `ai-service-core` | `8e23744` turbo init → `a14dce4` full Prisma schema → `d467706` `/api/me` + testable `createApp()` → `45e17bd`/`82bb7f1` streaming ingestion → `47d80f5` upload routes |
| 2 | `feat/exception-queue-loan-detail` | `2967735` exception + loan routes → `baf21fd` verified loans/audit/summary → `ddff0cd` S2 deny-by-default → `8b58a24` central CUID schema |
| 3 | `ai-service-core` | `9455df2` AI service + rate limiter + conflict detection → `b90607f` AI tests (32 green) → `25474a4` rate-limit leak fix, tape-scoped lookup → `c672964` default model `gemini-3.5-flash-lite` → `bd6c315` S4 strict rule schema → `c130c73` review hardening (partial index, orphan guards, `createManyAndReturn`) |
| 4 web | `dev` → `main` | `cafe3f3` consumer loan detail, search/pagination, real operator stats |
| 4 B | `phase-4b-backend-hardening` | `ca3aff3` PATCH tx fix → `4405350` consumer scoping → `d59b6c3` document-manifest service → `f8e0005` pre-ingestion dispatch → `d13d8be` review hardening → `662ec88` `buildApplyWindows` → `74504f1` batched manifest writes + strict headers |
| 5 | `final-touches` (public-data) | `8f7411a` types `fannie_mae`/`freddie_mac` → `b84b35f` pipe field-map + ingestion (tolerant gate, MMYYYY, fold) → `1296c05` uploads dispatch → `c58df15` dropzone/accepted formats → `62352b0` fold pending fix + 37 unit tests → `731c368` 3-test public-data integration (pipe → folded, validation, non-regression) → `6123cc3` conflict timeout 30s |

## 1. Tools Used
*   **Antigravity (Powered by Gemini 3.1 Pro / Claude Sonnet 4.6):** Used as our primary agentic architect and pair programmer. It helped design the system architecture, write the API contracts, generate the 48-hour implementation plan, and research library integrations (Better Auth).
*   **opencode CLI (GLM):** Drove Phase 4(B) backend hardening end-to-end — plan synthesis from `.context/`, implementation of the document-manifest pipeline, test suites, a senior-code-reviewer subagent pass, and this log's fact-checking. All commits on `phase-4b-backend-hardening` were produced under explicit human approval of the plan (three design decisions were put to the human: audit-event reuse vs new enum, manifest pre-ingestion vs post-processing, ff-merge strategy).
*   **Context7 MCP:** Live documentation pulls for Better Auth, Prisma 7 adapter APIs, and AI SDK versions.
*   **lefthook + ultracite (Biome):** Enforced as a pre-commit gate on every commit — lint, `prisma generate`, `turbo check-types`, `turbo build`. Several commits were initially rejected by this gate and fixed before landing.

## 2. Use Cases
We leveraged AI across the full stack lifecycle:
*   **Architecture & Planning:** Processing the 300-line hackathon prompt to generate a 6-phase implementation plan split between Frontend (Person A) and Backend (Person B).
*   **API & Schema Design:** Generating the full Prisma schema and Zod API contracts.
*   **Library Research:** Using Context7 via MCP (Model Context Protocol) to pull live documentation for `better-auth` integration with a decoupled Express backend.
*   **Phase 4(B) gap analysis:** AI audited the branch against `implementation-plan.md` Hours 33–37 and produced a correlation table (done / not started / latent bug) before any code was written — surfacing the unreachable `PATCH /loans/:id/fields` transaction and the explicitly deferred document-manifest branch (`ingestion.service.ts` Phase 4 comment).
*   **Adversarial code review:** A dedicated senior-code-reviewer subagent reviewed the full Phase 4(B) diff against the project's agent rules (G1–G5, S1–S5, E1–E5) and returned 1 Block + 4 Warns, all fixed in `d13d8be`.
*   **End-to-end verification:** AI scripted and executed the full 14-step demo flow against a live server (upload real `loan_tape.csv` → servicer conflicts → manifest apply → AI explain/decision → verify → consumer views → CSV export → RBAC negative checks), then cleaned up its test data in a single transaction.

## 3. Representative Prompts (5-10 required)
Here are some of the key prompts we used to drive the development:

1.  **System Architecture Design:**
    > *"based on the problem.md, I want you to design an architecture and flow.md as well as an api-contract.md . I want you to choose best possible arhcitecture setup for the same, you can grill me if you need more info. Frontend: Next.js, TypeScript, Tailwind, shadcn/ui, TanStack Query. Backend: Node.js, Express, TypeScript, Zod, Prisma. Database: PostgreSQL. auth: better auth."*
2.  **Implementation Planning:**
    > *"the development of this would be divided between two people A and B, A will be dealing mainly with the frontend part of the development, B will deal with everything else. Create a very thorough and detailed phase wise plan of implementation, showing clear segragation between the roles, we have 48 hrs. Ensure you take every feature, every guidelines into account."*
3.  **Enterprise Scalability Check:**
    > *"would we need a monorepo in the first place? wouldnt next.js by default work for it? ... what if the sheet is much more than 1000 rows, what if its a million"*
4.  **Dataset Reality Check (problem.md §4-6 vs ingestion):**
    > *"just downloaded one in /Downloads. check if thats the valid csv" — then realizing the public Freddie Mac sample at `problem.md:4` is pipe-delimited (`|`) with 108 columns while our ingestion at `ingestion.service.ts` expects comma `,` + 21-column header (`problem.md:6`). We asked whether the pipeline was "worthless" if it only handled one file type.*
5.  **Fault-Tolerance Audit (hackathon graceful handling):**
    > *"how fault tolerant and graceful handling is in our current ingestion pipeline in terms of the context of hackathon" — asking to evaluate stream vs in-memory, BOM, empty rows, failedRows cap, chunked `createMany(skipDuplicates)`, `markFailed` vs `processing` zombie.*
6.  **Synthetic Fixture Generation (problem.md §5 & §7):**
    > *"create me a csv for testing, we'll go with option A" — generating `loan_tape.csv` (137 rows + header, seed 42) covering all 15 intentional data issues at `problem.md:7` to land in either `failedRows` (ingestion) or validation exceptions.*
7.  **Phase 4(B) Plan with Decision Points (this session):**
    > *"pull the latest changes, then taking contxt of ./context, I want you to devise a thorough plan of implementation for implementing phase 4 (B) ... I want the most prisitne fool proof implementation with direct corelaton to the problem statement ... ensure post implementation of eevrything, you write unit and integration test cases accordingly."* — AI responded with a gap-analysis table, three explicit design questions (audit-event reuse, manifest model, base refresh), and only proceeded after the human answered.
8.  **Senior Review Invocation (this session):**
     > *"once the whole implementation is done, run senior code reveiwer and fix the relevant issues."* — produced the cross-chunk grouping Block finding below.
9.  **Public-Data Plan (problem.md §4 — public links + optional feature):**
     > *"I wat you to go trhough problem statement, apparently there are some public links given there, with optional feature mentioned. can you explain me what it is?"* → AI mapped §4 URLs (Fannie Mae SF LP + Freddie Mac SF Level, pipe 108-col, registration-gated) vs §5 synthetic comma 21-col and §52 Recommendation, clarifying public-data as optional stretch (alternative ingestion pipelines) with our prior rejection rationale.
10. **Public-Data Implementation Plan (complement existing pipeline):**
     > *"I want you to create a thorough plan of implementation to add this feature, ensuring we dont contradict problem statement and we complement our exisitng pipeline"* → AI researched ingestion/manifest precedent, sample `sf-loan-performance-data-sample.csv` (757 rows, 8 loans), drafted 7-phase plan with 4 explicit design questions (upload-path only, two fileTypes, fold + `unknown`, tolerant gate) and waited for human answers before coding.
11. **Public-Data Build with Tests → Review:**
     > *"start the implementation, ensure you commit as you go, write and execute unit and integration tests once u implement features. once done, run the sneior code reviewer and fix the relevant issues"* → implemented `field-map.ts` (MMYYYY, gate, `PUBLIC_DATA_FORMAT_REGISTRY`), `ingestion.service.ts` (pipe csv-parser, incremental contiguous-run fold, 5k chunk), route wiring, dropzone, 37 unit tests + 3 integration (pipe → folded + stale_record + non-regression), then senior review pass.

## 4. Human Review Process
*   **AI-Generated Code Percentage Estimate:** ~65% architecture/contracts/scaffolding by AI; ~35% human hardening (reviews, edge cases, logging, overwrite control, failedRows UX). Phase 4(B) was near-fully AI-executed but on a human-approved plan with human-supplied design decisions.
*   **Review Process:** We treated the AI as a junior developer proposing PRs. All AI-generated architectures were manually reviewed against the hackathon constraints (`problem.md` §1-16). We established explicit "Handoff Contracts" between Person A and Person B so that AI-generated frontend code could be tested against AI-generated backend endpoints independently. Every PR required `bun x ultracite check` + `turbo check-types` + `turbo build` via `lefthook` pre-commit.
*   **AI reviews AI:** Phase 4(B) added a second gate — a reviewer subagent with the project's own agent rules as its rubric. Its Block finding (cross-chunk manifest grouping) was a genuine correctness bug no human had spotted, in code that was already fully test-covered. Tests verified correctness only for contiguous small fixtures; the reviewer reasoned about the 5k-chunk boundary case.

### 4.1 What we faced per problem.md and how we verified
*   **§4 vs §5 dataset ambiguity (public pipe vs synthetic comma):** The organizer package (`loan_tape.csv` 1k-5k, 21 columns at §6) is comma-delimited with a header; the public Fannie/Freddie samples (§4) are registration-gated, large, and—critically—pipe-delimited (`|` with 108 columns) with no header (sample at `~/Downloads/sf-loan-performance-data-sample.csv`, 757 lines, `|100023020488|082009|R|Other|...|55000.00|...`). Our ingestion expects `csv-parser` + `BOM_REGEX` + comma + `loan_id,borrower_id,...source_system`. Uploading the raw public file would normalize every row to `failedRows: missing loan_id and borrower_id`. Per `problem.md:52` we standardized on the synthetic packet (Recommendation: *"Use synthetic organizer-provided dataset for judging"*). Header validation logs `[Ingestion] Detected CSV headers`, checks `KNOWN_COLUMNS`, and fails gracefully with `CSV header mismatch` instead of silent `done` with 0 rows.
*   **§6 strict 21-column schema vs tolerance:** The 21 fields are normative. `cleanString`/`parseDecimal`/`parseIntSafe` are tolerant (trim, BOM, commas, empty→null, garbage numbers→null) but `parseDateField` throws on `invalid date format <field>: <value>` so bad dates surface in `failedRows` with row number + rawData + reason, capped at 1,000 (`MAX_FAILED_ROWS_STORED`).
*   **§7 intentional issues split (ingestion vs validation):** 2 issues (missing ids, invalid date format) fail at ingestion (`failedRows`); the remaining 13 are caught by `validation.service.ts` (10 per-loan rules + 3 batch-scoped duplicate checks). Verified live: uploading `loan_tape.csv` yields `recordCount=137, failedCount=8`, summary `imported=129, passed=69, failed=60` with exceptions distributed across `balance_error 13, duplicate 15, date_error 5, invalid_state 5, missing_field 5, rate_out_of_range 5, stale_record 5, status_inconsistency 10` (critical 28 / high 15 / medium 15 / low 5).
*   **§8 Module A ingestion at scale (1k→1M rows):** `problem.md:8 Module A` requires streaming. AI first proposed in-memory `papaparse`; we rejected it (OOM) and kept `fs.createReadStream.pipe(csv-parser)` + chunk `5000` + `pause/resume` + `prisma.loan.createMany({ skipDuplicates: true })` + `UploadBatch.processedCount` checkpoint. Verified with 50k-row synthetic load; memory flat.
*   **§8 Module F audit trail / §10 AI controls (§9):** Every state transition writes `AuditLog` in the same transaction as the mutation (G3/E4), and operator polling is every 2s (`use-uploads.ts`). AI recommendations are stored on `Exception.aiRecommendation` but never silently applied — reviewer must `accept/edited/rejected` via `POST /exceptions/:id/decision`. Verified live with `MOCK_AI=true` fallback (model `mock-gemini-3.5-flash-lite`, confidence 0.84) followed by a `rejected` decision write.
*   **§8 consumer views + export (Phase 4):** Verified live end-to-end: `GET /api/loans` as consumer returns only verified loans (2 of 131); unverified detail → 403; `POST /verify` → `recordHash=ad36a913fd11c322…`; `GET /api/verified-loans` → `qualityScore=0.3`; CSV export streams with `Content-Disposition: attachment; filename="verified_loans_2026-08-27.csv"` (2 data rows, full canonical columns); RBAC negatives all correct (consumer verify/PATCH/AI → 403, operator PATCH → 403, no-auth → 401).

### 4.2 Test evidence (current state of `final-touches` + public-data)
*   **Unit (per-file runs, `bun test src/lib src/middleware src/routes src/services`):** 168 tests passing across 14 files (adds `public-data/field-map` 31, `public-data/ingestion` 6); prior 129 on `phase-4b` baseline. No failures when correctly scoped (see §4.2 contamination note).
*   **Integration (`bun run test:integration`, isolated `luma_test` DB after `migrate deploy`):** 24 tests passing across 6 suites — `auth-flow`, `uploads`, `loans` (consumer scoping + PATCH round-trip), `manifest` (apply/replay/orphan-crash/bad-headers), `ai` (MOCK_AI + conflicts + rate limit), `public-data` (freddie pipe → folded 2+1 bad, fannie pipe → 1, synthetic non-regression). The 5 `luma` DB integration tests (uploads) and public-data 3-test suite all green on this branch with the `luma_test` DB.
*   Aggregate single-process `bun test` over all files has pre-existing cross-file `mock.module` contamination (reproduced on pristine `main`); per-file or `test:integration` runs are the reliable signal.

## 5. What Was Rejected (Required)
We actively challenged and rejected AI output when it didn't meet enterprise standards.

*   **Rejected Output 1: In-Memory CSV Parsing (OOM Risk)**
    *   **What AI proposed:** Initially, the AI proposed using Next.js API routes or `papaparse` in Express to read the uploaded CSV files directly into memory before bulk-inserting them into PostgreSQL.
    *   **Why we rejected it:** We challenged the AI by asking how it would handle a 1-million-row CSV. The AI admitted that the initial in-memory design would cause an Out-Of-Memory (OOM) crash and Vercel serverless timeouts.
    *   **How we fixed it:** We forced a redesign to use a **Stream + Batch** architecture. We switched to `csv-parser` with `fs.createReadStream`, accumulating rows into chunks of 5,000, and pausing the stream to run `prisma.createMany`. We also added a `processedCount` cursor to the database to ensure the job could resume if the Express server crashed mid-stream.
*   **Rejected Output 2: Pipe-Delimited Public Data Without Header Validation (Silent Wrong Data)**
    *   **What AI proposed:** When we pointed the model at the public Freddie Mac sample (`sf-loan-performance-data-sample.csv`), it initially suggested widening `ingestion.service.ts` to accept any delimiter/header and inserting whatever parsed into `Loan`.
    *   **Why we rejected it:** That would ingest 757 pipe-rows as 757 loans with all-null fields (every row sharing empty `loan_id`), poisoning validation/verification and violating `problem.md:6`. `problem.md:52` says to use the synthetic packet for judging; public data is optional stretch.
    *   **How we fixed it:** We kept the strict 21-column `KNOWN_COLUMNS` contract but added explicit header validation: log detected headers, check `KNOWN_COLUMNS`, emit `CSV header mismatch: ... Expected loan_id, borrower_id...`, and fail the batch (`lastSuccessfulRowEnd`, `deleteMany` rollback, `status: failed`) instead of `done` with 0/758 valid. Plus a synthetic `loan_tape.csv` (137 rows, seed 42) was generated for the happy-path demo.
*   **Rejected Output 3: Validation `failedValidation = totalExceptions` (Inflated Counts)**
    *   **What AI proposed:** Early `routes/uploads.ts` summary set `failedValidation = exception.count` (exception rows), so a loan with 3 exceptions counted as 3 failures; `passedValidation = totalImported - totalExceptions` could go negative.
    *   **Why we rejected it:** `problem.md:8 Module B` semantics are per-loan (a loan either passes or fails); a reviewer counts distinct loans needing attention, not exception rows.
    *   **How we fixed it:** Changed to `prisma.loan.count({ where: { sourceBatchId, exceptions: { some: {} } } })` so `failedValidation` = distinct loans with ≥1 exception and `passedValidation = max(0, totalImported - failedValidation)`.
*   **Rejected Output 4 (Phase 4B): AI's Own First Manifest Design — Size-Triggered Chunk Flush (Cross-Chunk Corruption)**
    *   **What AI proposed:** The initial `document-manifest.service.ts` buffered manifest rows and flushed per 5k-row window, deciding each loan's `documentStatus` from whatever subset of its rows happened to be in the current window.
    *   **Why we rejected it:** Our own reviewer pass flagged it as a Block: any manifest over 5k rows can split one `loanId`'s rows across two windows, and the second window re-decides from its partial subset — flipping `missing` → `complete` while the first window's `missing_field` exception stays open. Silent financial-data corruption at exactly the scale the chunking was designed for; small demo files masked it.
    *   **How we fixed it (`d13d8be` + `662ec88`):** Whole-file per-loanId accumulation during streaming (rows are tiny triples — bounded memory), then `applyChunk` receives only *complete* loan groups via the pure `buildApplyWindows` helper (rows never split; oversized single groups get their own intact window). Covered by new unit tests for window invariants and an integration test.
*   **Rejected Output 6 (Public-Data Phase 5): Single-Loan Fold Counted as Empty (Pending-Run Bug)**
    *   **What AI produced:** Initial `public-data/ingestion.service.ts` checked `totalFoldedLoans === 0` after the streaming loop to decide "empty file" vs "all failed." With the contiguous-run fold, a file containing 3 rows for a single `loanId` leaves `currentLoan` pending (not yet pushed to `totalFoldedLoans`) so the check misfired and marked the batch `failed: empty — no valid rows` even though 3 rows were valid and would have been flushed in `finalizeSuccess`. The same bug made a 1-good + 5-bad file look like "all 5 failed."
    *   **Why we rejected it:** Unit test `folds 3 monthly rows for same loanId -> 1 inserted` (and the `sourceSystem matches` single-row test) failed — `expected 1 received 0` and `FAILED: empty` in the ingestion log. The `totalFailedRows` path also mis-counted pending. Small-fixture manual pipe parsing succeeded, proving the streaming logic was correct but the accounting was off by the pending run.
    *   **How we fixed it:** Changed the empty/all-failed guards to `pendingFolded = currentLoan !== null ? 1 : 0; effectiveFolded = totalFoldedLoans + pendingFolded` before deciding. Verified with the 6-test public-data unit suite (now 37 tests including field-map) and the 3-test public-data integration (freddie 5 raw → 2 folded +1 failed, fannie 1 row, synthetic non-regression) — all green.

*   **Rejected Output 5 (Phase 4B): Test Suite Accepted the Bound-Call PATCH Bug**
    *   **What AI found/failed:** The pre-existing `PATCH /api/loans/:id/fields` transaction destructured `tx.loan.update` off the delegate and called it unbound — a runtime `TypeError` before any edit could land — yet the shipped suites were green because no test exercised the happy path through the real transaction.
    *   **Why we rejected it:** Green suites with an unreachable primary mutation is exactly the "tests pass, product broken" failure judges probe.
    *   **How we fixed it (`ca3aff3`):** Direct typed `tx.loan.update` call, dead post-coercion negative check removed, and a new unit suite asserting the `FIELD_EDITED` audit payload (field/oldValue/newValue) plus a live integration round-trip proving DB mutation + audit rows commit atomically.

## 6. Lessons Learned
*   **Where AI helped most:** System design and boilerplate generation. The AI was exceptionally fast at translating a raw markdown problem statement into a structured set of database tables and REST endpoints. It also quickly scaffolds validation rules covering `problem.md:7`'s 15 intentional issues once the 21-column contract is pinned. In Phase 4(B) the highest-value AI contributions were *adversarial*: the gap analysis that found an unreachable mutation and the reviewer pass that found the cross-chunk corruption.
*   **Where human engineering judgment was necessary:** Scalability, fault tolerance, and data-contract fidelity. The AI tends to choose the "easiest" path first (loading a whole file into memory; accepting any delimiter/header; counting exceptions instead of loans; flushing partial groups at chunk boundaries). It requires strong human prompting to design for edge cases, crashes, and to keep the ingestion/validation boundary clean per `problem.md:7-8`. Two process rules earned their keep: (1) make the AI present design *decisions with trade-offs* and wait for the human before coding; (2) never trust green suites — ask what the tests *cannot* see (chunk boundaries, replay after soft failure, header-case drift).
